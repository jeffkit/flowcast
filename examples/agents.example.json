{
  "//": "flowx agent profile 示例。复制到 ~/.flowx/agents.json（机器级）或 <repo>/.flowx/agents.json（项目级覆盖）。",
  "//provider": "provider 仅对 BYO-LLM 执行器（recursive/aider/claude）有效；给 cursor/gemini/codex 配 provider 会 fail-fast。",
  "agents": {
    "recursive-deepseek": {
      "executor": "recursive",
      "provider": "deepseek",
      "maxSteps": 60
    },
    "claude-sonnet": {
      "executor": "claude",
      "model": "claude-sonnet-4"
    },
    "cursor-default": {
      "executor": "cursor",
      "model": "auto"
    }
  }
}
